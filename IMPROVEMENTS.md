# خطة تحسينات منصة مسارات

> الأسئلة مُرتَّبة حسب الأولوية. كل إجابة تُضاف هنا ثم يُنفَّذ كل شيء دفعةً واحدة.

---

## المجموعة A — الأمان وجودة الكود 🔴

### A1 — Zod Validation على الـ API
**المشكلة:** عدة routes تقبل بيانات بدون التحقق منها بـ schema محدد.
**القرار:** ✅ إضافة Zod validation على كل routes الإضافة والتعديل.

### A2 — حماية تسريب الحقول الحساسة
**المشكلة:** حذف `googleServiceAccountKeyEnc` و`telegramBotTokenEnc` يدوياً — إذا أُضيف حقل جديد قد يُنسى.
**القرار:** ✅ استخدام Drizzle select بحقول آمنة محددة مسبقاً بدلاً من الحذف اليدوي.

---

## المجموعة B — اتساق الواجهة 🟡

### B1 — استبدال browser confirm() بـ Dialog احترافي
**المشكلة:** ProjectRecords و GlobalSettings تستخدم `window.confirm()` القديم بدلاً من الـ Dialog الاحترافي المستخدم في بقية الصفحات.
**القرار:** ✅ استبدال كل confirm() بـ Dialog متناسق مع تصميم المنصة.

### B2 — نظام Toast موحّد
**المشكلة:** كل صفحة تعرض نتائج العمليات بـ ResultBox مضمّن يُزيح المحتوى. يوجد Toast في المشروع لكنه غير مُفعَّل بشكل موحّد.
**القرار:** ✅ تحويل كل ResultBox إلى Toast عائم من أسفل الشاشة.

### B3 — Skeleton Loading بدلاً من Spinner
**المشكلة:** كل الصفحات تعرض Loader2 مركزي واحد — يسبب layout shift مزعج.
**القرار:** ✅ إضافة Skeleton للجدول وبطاقات الإحصاء وصفحة السجلات.

---

## المجموعة C — ميزات UX احترافية 🟡

### C1 — Audit Log UI (سجل النشاط)
**المشكلة:** جدول `project_audit_log` موجود ومُملَّأ في قاعدة البيانات لكن لا توجد أي صفحة تعرضه.
**القرار:** ✅ تبويب جديد "سجل النشاط" داخل ProjectSettings.

### C2 — ربط الـ Charts بصفحة السجلات
**المشكلة:** الضغط على شريحة في الـ Pie Chart بالـ Dashboard لا يفتح السجلات مفلترة بهذه القيمة.
**القرار:** ✅ النقر على شريحة يفتح السجلات مفلترة بهذا الحقل والقيمة.

### C3 — بحث في قائمة المشاريع (Sidebar)
**المشكلة:** مع كثرة المشاريع تصبح القائمة الجانبية طويلة بدون بحث.
**القرار:** ✅ إضافة حقل بحث فوري فوق قائمة المشاريع في الـ Sidebar.

### C4 — Auto-save بـ LocalStorage في نماذج التسجيل
**المشكلة:** إذا أغلق المشارك المتصفح أثناء تعبئة نموذج طويل تضيع كل بياناته.
**القرار:** ✅ حفظ بيانات النموذج تلقائياً وإعادة تحميلها عند العودة.

---

## المجموعة D — ميزات محرر النماذج 🔴

### D1 — Conditional Fields (حقول مشروطة)
**المشكلة:** لا يمكن إظهار/إخفاء حقل بناءً على إجابة حقل آخر.
**القرار:** ⏭️ تجاوز — مؤجّل.

### D2 — Field Validation Rules (قواعد التحقق على الحقول)
**المشكلة:** لا يمكن للمسؤول تحديد قواعد مثل: حد أدنى/أقصى لعدد الأحرف، أو نمط regex معين.
**القرار:** ✅ إضافة خيارات min/max وregex لكل حقل في محرر الحقول.

### D3 — File Upload كنوع حقل جديد
**المشكلة:** نوع الحقول المتاحة لا يشمل رفع ملف أو صورة.
**القرار:** ✅ إضافة نوع حقل لرفع ملف (صورة/PDF/أي ملف).

### D4 — Drag-and-drop لإعادة ترتيب الحقول
**المشكلة:** ترتيب الحقول يتم بتعديل رقم يدوياً — لا توجد واجهة سحب وإفلات مرئية.
**القرار:** ✅ إضافة سحب وإفلات لإعادة ترتيب الحقول.

---

## ملخص القرارات النهائية

| # | البند | القرار |
|---|-------|--------|
| A1 | Zod validation على كل routes | ✅ |
| A2 | Drizzle safe select للحقول الحساسة | ✅ |
| B1 | استبدال confirm() بـ Dialog | ✅ |
| B2 | Toast موحّد بدلاً من ResultBox | ✅ |
| B3 | Skeleton Loading للجداول والبطاقات | ✅ |
| C1 | تبويب Audit Log في ProjectSettings | ✅ |
| C2 | Charts قابلة للنقر → السجلات مفلترة | ✅ |
| C3 | بحث في قائمة المشاريع بالـ Sidebar | ✅ |
| C4 | Auto-save بـ LocalStorage في نماذج التسجيل | ✅ |
| D1 | Conditional Fields | ⏭️ مؤجّل |
| D2 | Field Validation Rules (min/max/regex) | ✅ |
| D3 | File Upload كنوع حقل | ✅ |
| D4 | Drag-and-drop لترتيب الحقول | ✅ |

---
